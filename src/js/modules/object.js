export const person = {
    name: 'Arek',
    age: 24,
    pet: true,
    currentStatus: null
};

export const hello = (name) => {
    return `hello ${name} !! <3`;
};

const maciek = 'Maciek by default (without string)';

export default maciek;
